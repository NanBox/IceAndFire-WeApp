
<!--index.wxml-->
<view class="root">
  <view class="tab-bar">
    <view wx:for="{{tabs}}" wx:key="{{item}}" data-index="{{index}}" bindtap="handlerTabTap" class="tab {{activeTab==index?'active':''}}" style="width: {{stv.windowWidth/tabs.length}}px">
      <text>{{item}}</text>
    </view>
    <view style="width: {{stv.lineWidth}}px; left: {{stv.offset/tabs.length}}px" class="under-line {{!stv.tStart? 'withAnimate': ''}}"></view>
  </view>
  <view class="scroll-view">
    <view class="scroll-view-wrapper {{!stv.tStart? 'withAnimate': ''}}" bindtouchstart="handlerStart" catchtouchmove="handlerMove" bindtouchcancel="handlerCancel" bindtouchend="handlerEnd" style="left: -{{stv.offset}}px">
      <view wx:for="{{types}}" wx:for-item="type" wx:key="{{type}}" style="width: {{stv.windowWidth}}px;">
        <scroll-view style="height:100%" scroll-y>
          <view wx:for="{{contents}}" wx:key="{{item.id}}" wx:for-item="item">
            <view class="item " wx:if="{{item.firstType == type}}" bindtap="goDetail" data-name="{{item.name}}" data-img="{{item.img}}" data-html="{{item.html}}">
              <image class="img" src="{{baseUrl+item.img}}" mode="widthFix" />
              <view class="name">{{item.name}}</view>
              <view class="intro">{{item.intro}}</view>
            </view>
          </view>
        </scroll-view>
      </view>
    </view>
  </view>
</view>
<loading hidden="{{hideLoading}}">正在加载</loading>